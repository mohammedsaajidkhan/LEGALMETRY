// Frontend Tests
// Each person writes tests only for their own module's folder
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Placeholder test', () {
    expect(1 + 1, equals(2));
  });
}
